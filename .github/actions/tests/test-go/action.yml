name: Test Go
description: Runs Go tests

inputs:
  app-path:
    required: true
    description: string

runs:
  using: "composite"
  steps:
    - run: |
        cd "${{ inputs.app-path }}"
        go test ./...
      shell: bash